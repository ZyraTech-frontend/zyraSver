version: "3.8"

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: zyratech-api
    restart: unless-stopped
    ports:
      - "${API_PORT:-5000}:5000"
    env_file:
      - .env
    command: npm start
